package com.bachld.backend.repository;

import com.bachld.backend.model.ScreenshotCapture;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScreenshotCaptureRepository extends JpaRepository<ScreenshotCapture, Integer> {
}
