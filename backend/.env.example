JWT=your-secret-key-here
VEYON_RSA_PRIVATE_KEY=
VEYON_SECRET=veyon-default-secret-key-change-in-prod

# Encryption key for per-machine VNC passwords stored in DB.
# Generate with: openssl rand -base64 32
VNC_ENCRYPTION_KEY=change-me-vnc-encryption-key